<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>curl on Caius Theory</title><link>https://caiustheory.com/tag/curl/</link><description>Recent content in curl on Caius Theory</description><generator>Hugo -- gohugo.io</generator><language>en-gb</language><copyright>This work is licensed under a Creative Commons Attribution-ShareAlike 4.0 International License</copyright><lastBuildDate>Sat, 14 Sep 2019 18:10:00 +0100</lastBuildDate><atom:link href="https://caiustheory.com/tag/curl/index.xml" rel="self" type="application/rss+xml"/><item><title>curl --resolve</title><link>https://caiustheory.com/curl-resolve/</link><pubDate>Sat, 14 Sep 2019 18:10:00 +0100</pubDate><guid>https://caiustheory.com/curl-resolve/</guid><description>&lt;p&gt;Sometimes it&#39;s useful to be able to craft a request to one server, using a DNS name that&#39;s either not defined or currently pointed to a different IP. (Migrating webservers, testing a new webserver config out, etc.)&lt;/p&gt;
&lt;p&gt;Historically for HTTP calls this was easy, just set the &lt;code&gt;Host&lt;/code&gt; header as you make the http request to the IP directly:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt; curl -H &amp;quot;Host: caiustheory.com&amp;quot; http://10.200.0.1/
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;However, HTTPS throws a bit of a spanner in the works, if we just try to connect using an overridden &lt;code&gt;Host&lt;/code&gt; header, we get an error back from the server if it&#39;s not configured with a certificate for the IP address:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt; $ curl -H &amp;quot;Host: caiustheory.com&amp;quot; https://10.200.0.1/
curl: (51) SSL: no alternative certificate subject name matches target host name &#39;10.200.0.1&#39;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Usually at this point I&#39;d just start editing &lt;code&gt;/etc/hosts&lt;/code&gt; to add &lt;code&gt;10.200.0.1 caius.name&lt;/code&gt; to it and carry on testing. This is a pain when you&#39;re testing more than one server, or you&#39;re on a machine where you don&#39;t have root access to edit &lt;code&gt;/etc/hosts&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;In later versions of curl there&#39;s a solution for this built into the binary, in the form of the &lt;code&gt;--resolve&lt;/code&gt; flag. You can tell it to override the DNS lookup for a specific hostname/port combination. This in turn means that the correct host is forwarded to the server for the correct SSL certificate to be chosen to serve the request based on host.&lt;/p&gt;
&lt;p&gt;It takes the form &lt;code&gt;--resolve HOST:PORT:IP&lt;/code&gt; where &lt;code&gt;HOST&lt;/code&gt; is the human-friendly host, &lt;code&gt;PORT&lt;/code&gt; is the webserver&#39;s port (convention is 80 for HTTP, 443 for HTTPS) and IP being the destination IP you want to hit. (As opposed to the one in DNS currently.)&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;$ curl --silent --head --resolve caiustheory.com:443:10.200.0.1 https://caiustheory.com | head -n1
HTTP/2 200
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;And voila, you don&#39;t need to fiddle with editing &lt;code&gt;/etc/hosts&lt;/code&gt;. Just use &lt;code&gt;--resolve&lt;/code&gt; to hit a different IP for a given host.&lt;/p&gt;</description></item></channel></rss>